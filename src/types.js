// @flow

export type UserProfile = {
  email?: string,
  user_id: string,
  picture?: string,
  nickname?: string,
  updated_at: string,
  name: string,
  user_metadata?: Object,
  app_metadata?: Object,
}

